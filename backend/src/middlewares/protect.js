import { verifyAccessToken,verifyRefreshToken } from "../utils/token.js";

// export const protect=async(req,res)=>{
//     if()

//     //! cookie based
//     const decodedToken=verifyAccessToken(req.cookies.accessToken);
// }